class SVG {
    constructor(userShape, colorOfShape, colorOfText, text) {
       this.colorOfShape = colorOfShape,
        this.userShape = userShape,
        this.colorOfText = colorOfText
        this.text = text
    }
};

module.exports = SVG;